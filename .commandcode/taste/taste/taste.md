# Taste
- Communicates in Korean; expects assistant responses in Korean (code, docs, and commentary). Confidence: 0.9
- Keeps `[project] dependencies` in pyproject.toml strictly runtime-only; development tooling belongs in a separate dev dependency group (uv `[dependency-groups]` / `--dev`). The standard dev toolchain is pytest (tests), ruff (lint), and pyright (type checking) — stated explicitly as "이 셋은 넣어야지" (these three must be included). Confidence: 0.8
- Prefers implementing exactly per provided spec documents (e.g., spec-1.md) rather than improvising behavior. Confidence: 0.8
- Wants end-to-end verification: install the package with the real install command and run the actual CLI to confirm outputs are produced, not just unit tests. Confidence: 0.9
- Expects no leftover artifacts after successful runs: no lock files, no temp files, no work directories; intermediate files are kept on failure (for resume) and deleted on success. Confidence: 0.9
- CLI tools must return proper exit codes (0 = success, 1 = failure) so shell scripts can rely on them. Confidence: 0.9
- Prefers optional extra artifacts (e.g., metadata.json) to be off by default, enabled only via an explicit flag. Confidence: 0.7
- Prefers the Python API to return a rich, typed result object (Pydantic model) that echoes both the input file and all options used, instead of a bare list of output paths. Confidence: 0.9
- Prefers API results to split files into `output` (requested final outputs) vs `leftover` (info files like checkpoints, and artifacts kept after failure like mp3s) so callers can use leftover for cleanup; errors should still return a result with leftover rather than raising. Confidence: 0.9
- Prefers a `--temp-dir PATH` option so all intermediate/temp files are isolated into a designated directory (cleaned on success, kept on failure) instead of being created next to the output files. Confidence: 0.9
- Tunes subtitle/text output for Korean readability: SRT/SPK max line width widened to 30 chars (2x of 15) and TXT wrap narrowed to 40 chars (2/3 of 60); expects these format parameters to remain tunable for fine-tuning. Confidence: 0.7
- Expects TXT transcripts to be broken into paragraphs based on speech pauses (defaults tuned to line break 1.0s gap, paragraph break 2.5s gap, lowered from the original 2.0/5.0 after they never triggered on real content); a pipeline that drops word timestamps and therefore cannot apply paragraph rules is considered a bug. Confidence: 0.9
- Prefers caching the full API transcription result (text + word timestamps + speakers) into `<input>.transcript.json`, kept on by default, so .spk/.srt/.txt can be re-rendered locally without re-calling the API — explicitly for format fine-tuning without extra API cost. Confidence: 0.9
- Distinguishes functional artifacts (e.g., transcript.json enabling re-render: default on, removed via an explicit `--no-transcript` flag) from purely informational artifacts (e.g., per-chunk metadata checkpoints: default off). Confidence: 0.7
- CLI option names must match the actual output file extension/format exactly (`--[no-]metadata-json` for `.metadata.json`, `--[no-]transcript-json` for `.transcript.json`), not generic names like `--metadata` or `--no-transcript`. Confidence: 0.9
- Prefers bidirectional boolean CLI flags in the `--[no-]X` form (argparse.BooleanOptionalAction) over one-way `--no-X` disable-only flags. Confidence: 0.9
- Prefers action-oriented names for mutable boolean options (`create_metadata_json = True|False` form), applied consistently across API params, internal functions, and Pydantic model fields; plain noun booleans like `metadata = True|False` are considered unclear. Confidence: 0.9
- When audio is split into chunks (each with reset local timestamps), global subtitle timing sync (ffsubsync) must run once on the merged full-audio timeline after restoring global offsets — not per chunk. Chunk-local timing resets are expected to be compensated in the merge step. Confidence: 0.8
- Expects the package to be distributed on PyPI and runnable via the standard `uvx --from <package> <command>` usage; all runtime dependencies must be declared in `[project] dependencies` so they auto-install and the user never needs to add `--with` extras. Confidence: 0.8
- Cares that large dev-only files (e.g., samples/ media used for testing) never end up in the installed package — asked explicitly whether `uv` install would pull in the big files under samples/; expects the wheel to contain only the package code (via `[tool.hatch.build.targets.wheel] packages = [...]`), so installing the tool is lean and sample/dev scripts stay out of the distribution. Confidence: 0.6
- Manages version from a single source of truth in pyproject.toml; expects CLI `--help` and a standard `-v|--version` flag to display that version (even if it means reassigning a conflicting `-v` short flag from verbose to version), not hardcoded copies in `__init__.py`. Confidence: 0.8
- Prefers the CLI to ship a short alias/shortcut for the primary command (`gemini-transcribe`) via `[project.scripts]`, so the shorter name works interchangeably for everyday use — but the alias must be distinctive and not overly short/ambiguous: user explicitly rejected `gt` as "too short" and requested `gtw` instead. Confidence: 0.8
- Wants partial custom mappings (e.g., `--speakers` speaker-name map) to degrade gracefully: apply the provided mappings, keep raw ids for unmapped entries, still write the output file, and only then emit a warning listing the uncovered items — never abort over an incomplete mapping. Confidence: 0.8
- Wants warnings to be actionable: print the full current state (e.g., complete speaker map with mapped + unmapped entries) plus a copy-pasteable recommended re-run command that fills missing entries with editable placeholders (e.g., `Name#` for the user to edit). Confidence: 0.8
- Wants the CLI to always print total elapsed execution time at the end of a run (on both success and failure paths), measured with monotonic time. Confidence: 0.8
- Wants per-API-call timing telemetry persisted into transcript.json — each entry with chunk index, attempts/retry count, started_at, duration_secs, status (and error on failure) — plus an aggregated `total_api_secs` and `call_count` summary, so every transcription run is auditable. Confidence: 0.8
- Prefers concise READMEs: explicitly tired of long README files; wants a short Quick Start containing only the essential user-run commands (get API key → install tool → run the CLI), a separate explanation section afterwards, and full option details deferred to `--help` rather than duplicated in the doc. Non-essential steps get cut entirely — the demo-file download step was later deleted from the Quick Start (users point the tool at their own local file instead). Confidence: 0.9
- READMEs must show equivalent commands for both Windows (PowerShell) and Linux/macOS side by side, not just one platform. Confidence: 0.8
- Wants README usage examples to document both ways of supplying a config value — via environment variable (e.g., `GEMINI_API_KEY`) and via the explicit CLI flag (e.g., `--gemini-api-key VALUE`) — so users can pick either. Confidence: 0.6
- Prefers the MIT license for projects. Confidence: 0.6
- Wants distributed packages to support a broad Python version range (e.g., 3.10–3.14) with no reinstall burden on end users: declare `requires-python` in pyproject.toml, keep the code free of syntax newer than the minimum supported version, and rely on uv to reuse an installed compatible Python or auto-download one. Confidence: 0.8
- Expects the documented install command to be self-sufficient and accurate: one `uv tool install` must install every entry point declared in `[project.scripts]` (the full command and its shortcut together), with no extra separate install steps; a README command that errors out (e.g., the `uv tool install --from <pkg> <toolname>` name-mismatch form) is treated as a bug to be found and fixed by actually running the command. Re-asked about this across PyPI and Git-URL install forms, expecting the no-tool-name form (`uv tool install git+...`) to install everything. Confidence: 0.8
- Wants generated scripts written without comments — explicitly requested "주석은 넣지마" (no comments) when converting publish.bat to publish.sh. Confidence: 0.7
- Prefers one small wrapper script per dev tool at the repo root (e.g., `run_pytest.sh`, `run_ruff.sh`, `run_pyright.sh`) that delegates to `uv run --group dev <tool> "$@"` with `set -e` and `cd "$(dirname "$0")"`, so each lint/test/type-check tool is runnable by a plain `./run_*.sh` without remembering the uv invocation. Confidence: 0.8
- Maintains parallel cross-platform release scripts for the same workflow (e.g., publish.bat for Windows and publish.sh for bash), expecting a faithful behavior-preserving port rather than a rewrite; expects requested changes to be applied to both scripts in parallel (explicitly referred to updating "publish.*") so the two stay in sync. Confidence: 0.8
- Wants publish/release scripts to run pre-flight auth/credential checks before building or publishing: verify required secrets and logins (e.g., `UV_PUBLISH_TOKEN` env var set, `gh auth status` for GitHub) and fail fast with a clear actionable message (e.g., "Run 'gh auth login' first") and exit 1, rather than discovering the missing auth midway through the release. Confidence: 0.8
- Prefers to reuse existing authentication across environments instead of re-authenticating: when porting a workflow from a Windows host to WSL2, expects the assistant to detect what is already configured (e.g., `gh auth git-credential` for GitHub push) and identify only the missing credential (e.g., `UV_PUBLISH_TOKEN`) to carry over, so the script runs with no additional auth prompts. Confidence: 0.6
- Sensitive credentials (e.g., GEMINI_API_KEY) must never be logged in full — log only a masked form showing the first and last 4 characters with `*` in between (e.g., `Using GEMINI_API_KEY=AIza****abcd`), and fully `*`-mask keys of 8 chars or fewer. Confidence: 0.9
- Designs for the most likely first-user failure mode: since the top failure for new users is a missing or mismatched API key, expects the tool to surface key state early and clearly — log the masked key actually in use, and print a distinct WARNING with the remedy when the key is unset, so users can immediately debug which key was picked up. The key log must appear exactly once: a duplicate "Using GEMINI_API_KEY" line printed from both CLI startup and the validation point was reported as a bug ("두 번 반복되거든... 한번 표시되도록") — each status message should be logged from a single code path (the real validation point), never twice. Confidence: 0.8
- API rate-limit/quota errors (HTTP 429) must fail fast: abort immediately on the first 429 rather than retrying with backoff, since backoff cannot resolve a daily quota and the friendly hint should surface right away ("429 에러를 만났는데 왜 4번 시도하지? 한번이라도 만나면 즉시 중단해"); only transient server errors (500/502/503/504) and network timeouts are retried. Confidence: 0.9
- Prefers the global `-q` (quiet) flag on every `uv`/`uvx` invocation to suppress noise — `uv -q run --group dev ...`, `uv -q build`, `uv -q publish`, `uv -q tool install ...`, `uvx -q --from/--with ...` — and expects this to be applied consistently across ALL usages, both scripts (.sh/.bat) and docs (README, spec files), so commands documented and commands run stay identical. Confidence: 0.8
- Prefers internal/debugging parameters (e.g., the mp3 chunk-splitting unit) to be overridable via a CLI flag (e.g., `--chunk-secs`) instead of being locked into the built-in algorithm — explicitly requested "instead of cutting by algorithm, make it specifiable from the command line" for internal debugging, and expects it threaded through the API param, the CLI option, and the split-plan logic. Confidence: 0.7
- When introducing a new configurable mode/option, wants the sample scripts updated to exercise it (e.g., standard mode vs `--chunk-secs 60` samples) and verification that the new mode's output is identical to the default mode's output (diffing the generated .srt/.txt) — the option must be behavior-preserving, not produce different transcripts. Confidence: 0.6
- Expects the repo's lint/type-check state to be fully clean: when quality-gate tools (ruff, pyright) report errors or warnings, wants them all resolved — auto-fixes (`ruff --fix`, `--unsafe-fixes`) plus targeted manual fixes — until the gate passes, rather than leaving known failures outstanding; intentional patterns (e.g., broad `except Exception`, best-effort except-pass cleanup) are only acceptable with explicit `# noqa` annotations. Confidence: 0.7
- Expects the repo's lint/type-check state to be fully clean: when quality-gate tools (ruff, pyright) report errors or warnings, wants them all resolved — auto-fixes (`ruff --fix`, `--unsafe-fixes`) plus targeted manual fixes — until the gate passes, rather than leaving known failures outstanding; intentional patterns (e.g., broad `except Exception`, best-effort except-pass cleanup) are only acceptable with explicit `# noqa` annotations. Confidence: 0.7
- Prefers make-like freshness semantics for skip conditions in tools that generate output files from a source: outputs count as valid only when every target exists AND every target is at least as new as the newest source; regenerate whenever any source is newer than a target or any target is missing (stated explicitly: "소스 파일이 타겟보다 더 새것이면 생성해. 타겟이 없으면 생성해... 타겟이 나중에 생긴 경우에만 유효한 결과 파일로 간주한다"). Confidence: 0.7
- When a multi-output regeneration run rewrites only a subset of outputs (some stale, some already fresh), wants the log to explicitly distinguish changed vs. unchanged files: separate log lines listing regenerated outputs (e.g., "Re-rendered from transcript: ...") and untouched ones (e.g., "Unchanged (already up to date): ..."). Expects only stale targets to be rewritten while fresh targets are preserved and reported — per-target staleness, not a blanket rewrite of all active outputs. Confidence: 0.8
- Wants user-facing error messages for expected API failures (e.g., HTTP 429 quota exceeded) to be friendly and actionable: restate the relevant limits (max 2 calls/min, max 30 min/call, max 25 calls/day) and give clear next-step guidance ("try again tomorrow" for daily quota, "wait and retry" for short-term rate limits), rather than a raw exception dump. Confidence: 0.8
- Wants READMEs to document the service's rate limits/quota constraints up front (e.g., Gemini free tier: max 2 API calls/min, max 30 min of audio per call, max 25 calls/day) along with the failure mode when exceeded (HTTP 429), so users know the limits before hitting them. Confidence: 0.7
- Prefers user-facing CLI terminology (usage/help positional argument names, option names) to use plain, conventional terms rather than invented jargon — explicitly asked to rename the CLI positional argument `metapath` to plain `path` because seeing "metapath" in the usage line was confusing ("뭐지 하게 되네"). Confidence: 0.8
- When renaming a user-facing term, expects the old term to be fully removed from the codebase — identifiers, docstrings, and comments included — verified with a repo-wide grep showing zero remaining occurrences, not just the CLI help string. Confidence: 0.6
- Wants API error output to include the official documentation reference URL for the model/API in use (e.g., `https://ai.google.dev/gemini-api/docs/models/gemini-3.5-transcribe`) whenever an API call fails — both inside the 429 quota hint and as a `Gemini API call failed. Reference: <URL>` line for any other API error — so users have a direct link to look up details ("api 호출 시 에러 만나면 ref url로 ... 여기 보여주자"). Confidence: 0.7
- Prefers canonical BCP-47 language codes per official vendor documentation over shortened/legacy codes — Korean must be `ko-KR`, not `ko` (per the Gemini model docs); expects the corrected code applied everywhere (CLI default, API params, Pydantic model fields, spec docs) and verified end-to-end in the actual API request (`language_codes: ['ko-KR']`). Confidence: 0.7
- Prefers large binary/media files (e.g., `*.mp3`) in the git repo to be tracked with Git LFS (`git lfs track "*.mp3"` → `.gitattributes` with `*.mp3 filter=lfs diff=lfs merge=lfs -text`) so the repo stores LFS pointers instead of binary blobs; expects a staged-file verification that newly added matching files become LFS pointers (not raw binaries). Confidence: 0.8
- Cares that the published package is usable on all major platforms (Windows, macOS, Linux), not just the OS where it was built — asked explicitly whether a Linux-built wheel from publish.sh is Linux-only; expects platform-independent distribution (pure-Python `py3-none-any` wheel) with OS-specific binaries (e.g., ffmpeg) handled as runtime dependencies resolved at install time. Confidence: 0.5
- Wants GitHub and PyPI to cross-link each other so users can jump between the two: the README (right after the one-line description) gets PyPI and GitHub badges/links, and pyproject.toml gets `[project] urls` metadata (Homepage, Repository, Bug Tracker) so the PyPI page shows the GitHub links — requested explicitly ("pypi와 github에서 서로 리포로 점프할 수 있도록") and verified by checking the built wheel METADATA for `Project-URL` entries. Confidence: 0.8
